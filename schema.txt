 Game  Scheme and note


Class Card(){
int id; // Two cards must have same ID to identify them
var imgFront;
var imgBack;
function instanciate(){} // Create DOM
function flip(){} // Flip the card 
}

We can use Array.some() to check if a card is returned by writing a callback that check it.
